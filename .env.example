# Flask Server Configuration
FLASK_APP=app.py
FLASK_ENV=development
PORT=5000

# MySQL Database Connection Settings (Optional: App falls back to SQLite event_management.db if empty)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=event_management
